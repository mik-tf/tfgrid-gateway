<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>🚀 ThreeFold Grid Gateway - Live Demo</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #333;
            line-height: 1.6;
            min-height: 100vh;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            margin-bottom: 30px;
            color: white;
        }

        .header h1 {
            font-size: 2.5rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .header p {
            font-size: 1.2rem;
            opacity: 0.9;
        }

        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .card {
            background: white;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 8px 32px rgba(0,0,0,0.1);
            border: 1px solid rgba(255,255,255,0.2);
        }

        .card h3 {
            color: #2c3e50;
            margin-bottom: 20px;
            font-size: 1.4rem;
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .card h3::before {
            font-size: 1.2rem;
        }

        .info-grid {
            display: grid;
            grid-template-columns: 1fr 2fr;
            gap: 12px;
            align-items: center;
        }

        .label {
            font-weight: 600;
            color: #555;
            font-size: 0.9rem;
        }

        .value {
            font-family: 'Monaco', 'Menlo', monospace;
            background: #f8f9fa;
            padding: 8px 12px;
            border-radius: 6px;
            font-size: 0.85rem;
            word-break: break-all;
            border: 1px solid #e9ecef;
        }

        .value.ipv6 {
            font-size: 0.75rem;
            line-height: 1.2;
        }

        .status {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .status.enabled {
            background: #d4edda;
            color: #155724;
        }

        .status.active {
            background: #d1ecf1;
            color: #0c5460;
        }

        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 15px;
            margin-top: 20px;
        }

        .feature {
            text-align: center;
            padding: 15px;
            background: #f8f9fa;
            border-radius: 8px;
            border: 1px solid #e9ecef;
        }

        .feature::before {
            font-size: 1.5rem;
            display: block;
            margin-bottom: 8px;
        }

        .footer {
            text-align: center;
            margin-top: 30px;
            color: white;
            opacity: 0.8;
        }

        .footer a {
            color: white;
            text-decoration: underline;
        }

        .vm-link {
            display: inline-block;
            background: #007bff;
            color: white;
            padding: 8px 16px;
            border-radius: 6px;
            text-decoration: none;
            font-weight: 600;
            transition: background-color 0.3s ease;
        }

        .vm-link:hover {
            background: #0056b3;
            text-decoration: none;
            color: white;
        }

        @media (max-width: 768px) {
            .grid {
                grid-template-columns: 1fr;
            }

            .info-grid {
                grid-template-columns: 1fr;
                gap: 8px;
            }

            .header h1 {
                font-size: 2rem;
            }

            .vm-link {
                display: block;
                text-align: center;
                margin-top: 5px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🚀 ThreeFold Grid Gateway</h1>
            <p>IPv4 Gateway for IPv6-Only VMs - Live Demo</p>
        </div>

        <div class="grid">
            <div class="card">
                <h3>🌐 Network Configuration</h3>
                <div class="info-grid">
                    <div class="label">Gateway Type:</div>
                    <div class="value">{{ gateway_type | default('gateway_nat') | title }}</div>

                    <div class="label">Network:</div>
                    <div class="value">{{ network | default('test') }}</div>

                    <div class="label">Public IPv4:</div>
                    <div class="value">{{ ansible_default_ipv4.address | default('N/A') }}</div>

                    <div class="label">Hostname:</div>
                    <div class="value">{{ ansible_hostname | default('gateway') }}</div>
                </div>
            </div>

            <div class="card">
                <h3>🔒 Secure Networking</h3>
                <div class="info-grid">
                    <div class="label">WireGuard IP:</div>
                    <div class="value">{{ wireguard_ip | default('10.1.3.2') }}</div>

                    <div class="label">Mycelium IP:</div>
                    <div class="value ipv6">{{ mycelium_ip | default('Not assigned') }}</div>

                    <div class="label">IP Forwarding:</div>
                    <div class="status enabled">Enabled ✓</div>

                    <div class="label">Firewall:</div>
                    <div class="status active">Active ✓</div>
                </div>
            </div>

            <div class="card">
                <h3>🖥️ Internal VMs</h3>
                <div class="info-grid">
{% for host in groups['internal'] %}
                    <div class="label">VM {{ host }} ({{ hostvars[host]['wireguard_ip'] }}):</div>
                    <div class="value">
                        <a href="http://{{ ansible_default_ipv4.address }}/vm{{ host }}/" target="_blank" class="vm-link">
                            http://{{ ansible_default_ipv4.address }}/vm{{ host }}/ ↗️
                        </a>
                    </div>
{% endfor %}

                    <div class="label">WireGuard IPs:</div>
                    <div class="value">
{% for host in groups['internal'] %}VM {{ host }}: {{ hostvars[host]['wireguard_ip'] }}{% if not loop.last %} | {% endif %}{% endfor %}
                    </div>

                    <div class="label">Mycelium IPs:</div>
                    <div class="value ipv6">
{% for host in groups['internal'] %}
                        VM {{ host }}: {{ hostvars[host]['internal_' + host + '_mycelium_ip'] | default('N/A') }}{% if not loop.last %}<br>{% endif %}
{% endfor %}
                    </div>
                </div>
            </div>

            <div class="card">
                <h3>⚙️ Gateway Features</h3>
                <div class="features">
                    <div class="feature" title="NAT/Masquerading">🔥<br>NAT/Masquerading</div>
                    <div class="feature" title="Firewall (nftables)">🛡️<br>Firewall (nftables)</div>
                    <div class="feature" title="WireGuard VPN">🔐<br>WireGuard VPN</div>
                    <div class="feature" title="Mycelium Network">🌐<br>Mycelium Network</div>
                    <div class="feature" title="Path-Based Routing">🌐<br>Path-Based Routing</div>
                    <div class="feature" title="Reverse Proxy">⚖️<br>Reverse Proxy</div>
                </div>
            </div>

            <div class="card">
                <h3>📊 System Information</h3>
                <div class="info-grid">
                    <div class="label">Uptime:</div>
                    <div class="value">{{ ansible_uptime_seconds | int | default(0) }} seconds</div>

                    <div class="label">Load Average:</div>
                    <div class="value">{{ ansible_loadavg['1-min'] | default('N/A') }}</div>

                    <div class="label">Memory:</div>
                    <div class="value">{{ (ansible_memtotal_mb | int | default(0)) }} MB total</div>

                    <div class="label">Architecture:</div>
                    <div class="value">{{ ansible_architecture | default('x86_64') }}</div>
                </div>
            </div>
        </div>

        <div class="footer">
            <p>
                🚀 <strong>ThreeFold Grid Gateway Demo</strong> |
                <a href="/api/status" target="_blank">JSON API</a> |
                <a href="/health" target="_blank">Health Check</a>
            </p>
            <p style="margin-top: 10px; font-size: 0.9rem;">
                Real-time status of your IPv4 gateway providing internet access to IPv6-only VMs
            </p>
        </div>
    </div>
</body>
</html>