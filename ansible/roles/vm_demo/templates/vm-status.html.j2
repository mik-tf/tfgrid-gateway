<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>VM {{ vm_id | default(inventory_hostname) }} - ThreeFold Grid</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            color: #333;
            min-height: 100vh;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            text-align: center;
            color: white;
            margin-bottom: 30px;
        }

        .header h1 {
            font-size: 3rem;
            margin-bottom: 10px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
        }

        .header p {
            font-size: 1.2rem;
            opacity: 0.9;
        }

        .vm-badge {
            display: inline-block;
            background: rgba(255,255,255,0.2);
            padding: 10px 20px;
            border-radius: 25px;
            margin-bottom: 20px;
            font-size: 1.1rem;
            font-weight: bold;
        }

        .status-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .status-card {
            background: white;
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .status-card:hover {
            transform: translateY(-5px);
        }

        .status-card h3 {
            color: #4facfe;
            margin-bottom: 20px;
            font-size: 1.4rem;
            border-bottom: 3px solid #4facfe;
            padding-bottom: 10px;
        }

        .status-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
            padding: 10px 0;
            border-bottom: 1px solid #eee;
        }

        .status-item:last-child {
            border-bottom: none;
            margin-bottom: 0;
        }

        .status-label {
            font-weight: 600;
            color: #555;
            font-size: 0.95rem;
        }

        .status-value {
            font-family: 'Courier New', monospace;
            background: #f8f9fa;
            padding: 5px 12px;
            border-radius: 6px;
            font-size: 0.9rem;
            font-weight: 500;
            color: #2c3e50;
            max-width: 200px;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .status-value.ip {
            background: #e8f5e8;
            color: #27ae60;
            font-weight: bold;
        }

        .network-info {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            border-radius: 10px;
            margin-bottom: 20px;
        }

        .network-info h3 {
            margin-bottom: 15px;
            font-size: 1.2rem;
        }

        .network-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 15px;
        }

        .network-item {
            background: rgba(255,255,255,0.1);
            padding: 10px;
            border-radius: 8px;
            text-align: center;
        }

        .network-item .label {
            font-size: 0.8rem;
            opacity: 0.8;
            margin-bottom: 5px;
        }

        .network-item .value {
            font-size: 1rem;
            font-weight: bold;
            font-family: 'Courier New', monospace;
        }

        .gateway-connection {
            background: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            border-left: 5px solid #4facfe;
        }

        .gateway-connection h3 {
            color: #4facfe;
            margin-bottom: 15px;
        }

        .connection-status {
            display: flex;
            align-items: center;
            margin-bottom: 10px;
        }

        .status-indicator {
            width: 12px;
            height: 12px;
            border-radius: 50%;
            background: #27ae60;
            margin-right: 10px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.5; }
            100% { opacity: 1; }
        }

        .footer {
            text-align: center;
            color: white;
            margin-top: 30px;
            opacity: 0.8;
        }

        .footer a {
            color: white;
            text-decoration: underline;
        }

        .api-link {
            display: inline-block;
            margin-top: 15px;
            padding: 8px 16px;
            background: rgba(255,255,255,0.2);
            border-radius: 5px;
            text-decoration: none;
            color: white;
            font-weight: 500;
            font-size: 0.9rem;
        }

        .api-link:hover {
            background: rgba(255,255,255,0.3);
        }

        @media (max-width: 768px) {
            .header h1 {
                font-size: 2rem;
            }

            .status-grid {
                grid-template-columns: 1fr;
            }

            .network-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>🖥️ VM {{ vm_id | default(inventory_hostname) }}</h1>
            <p>ThreeFold Grid Internal Virtual Machine</p>
            <div class="vm-badge">VM {{ vm_id | default(inventory_hostname) }}</div>
        </div>

        <div class="network-info">
            <h3>🌐 Network Configuration</h3>
            <div class="network-grid">
                <div class="network-item">
                    <div class="label">WireGuard IP</div>
                    <div class="value">{{ wireguard_ip | default('N/A') }}</div>
                </div>
                <div class="network-item">
                    <div class="label">Mycelium IP</div>
                    <div class="value">{{ mycelium_ip | default('N/A') }}</div>
                </div>
            </div>
        </div>

        <div class="gateway-connection">
            <h3>🚀 Gateway Connection</h3>
            <div class="connection-status">
                <div class="status-indicator"></div>
                <span>Connected to {{ gateway_type | default('gateway_nat') }} gateway</span>
            </div>
            <div class="status-item" style="border: none; margin: 0;">
                <span class="status-label">Gateway Network:</span>
                <span class="status-value">{{ network | default('test') }}</span>
            </div>
        </div>

        <div class="status-grid">
            <div class="status-card">
                <h3>⚙️ System Information</h3>
                <div class="status-item">
                    <span class="status-label">Hostname:</span>
                    <span class="status-value">{{ ansible_hostname }}</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Architecture:</span>
                    <span class="status-value">{{ ansible_architecture | default('N/A') }}</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Distribution:</span>
                    <span class="status-value">{{ ansible_distribution | default('N/A') }} {{ ansible_distribution_version | default('') }}</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Kernel:</span>
                    <span class="status-value">{{ ansible_kernel | default('N/A') }}</span>
                </div>
            </div>

            <div class="status-card">
                <h3>📊 Runtime Information</h3>
                <div class="status-item">
                    <span class="status-label">Uptime:</span>
                    <span class="status-value">{{ ansible_uptime_seconds | default('N/A') }} seconds</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Load Average:</span>
                    <span class="status-value">{{ ansible_loadavg['1min'] | default('N/A') }}</span>
                </div>
                <div class="status-item">
                    <span class="status-label">Memory:</span>
                    <span class="status-value">{{ ansible_memory_mb['real']['total'] | default('N/A') }} MB total</span>
                </div>
                <div class="status-item">
                    <span class="status-label">CPU Cores:</span>
                    <span class="status-value">{{ ansible_processor_count | default('N/A') }}</span>
                </div>
            </div>
        </div>

        <div class="footer">
            <p>
                <strong>ThreeFold Grid VM {{ vm_id | default(inventory_hostname) }}</strong><br>
                This VM is connected to the ThreeFold Grid network via {{ gateway_type | default('gateway_nat') }} gateway
            </p>
            <a href="/api/vm-info" class="api-link">📡 View JSON API</a>
            <p style="margin-top: 10px; font-size: 0.9rem;">
                Part of the ThreeFold Grid Gateway Demo System<br>
                <a href="https://github.com/mik-tf/tfgrid-gateway" target="_blank">View Gateway Source Code</a>
            </p>
        </div>
    </div>
</body>
</html>